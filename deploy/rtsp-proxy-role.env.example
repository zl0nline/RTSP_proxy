# Install one copy per role as rtsp-proxy-<role>.env.
# Suggested initial loopback health ports:
# worker=8101, reconciler=8102, probe=8103, collector=8104.
RTSP_PROXY_HTTP_HOST=127.0.0.1
RTSP_PROXY_HTTP_PORT=8101
RTSP_PROXY_RECONCILE_INTERVAL_SECONDS=1
RTSP_PROXY_NODE_RUNTIME_SOCKET=/run/rtsp-proxy-node-runtime/control.sock
RTSP_PROXY_NODE_RUNTIME_TIMEOUT_SECONDS=60
# For the collector role only, override the socket with the read-only helper:
# RTSP_PROXY_NODE_RUNTIME_SOCKET=/run/rtsp-proxy-node-metrics/metrics.sock
RTSP_PROXY_COLLECTOR_INTERVAL_SECONDS=5
